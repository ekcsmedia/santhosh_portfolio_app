import 'package:flutter/material.dart';

class FirebaseDBHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Firebase DB CRUD Home'),),
    );
  }
}


