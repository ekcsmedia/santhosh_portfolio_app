import 'package:flutter/material.dart';

class FirebaseAuthHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Firebase Auth Home'),),
    );
  }
}


