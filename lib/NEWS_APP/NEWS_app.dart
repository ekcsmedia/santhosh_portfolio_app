import 'package:flutter/material.dart';

class NewsAppHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('NEWS APP HOME'),),
    );
  }
}


