import 'package:flutter/material.dart';

class RecipeAppHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('RECIPE APP HOME'),),
    );
  }
}


