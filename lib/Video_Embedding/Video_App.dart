import 'package:flutter/material.dart';

class VideoAppHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('VIDEO APP HOME'),),
    );
  }
}


